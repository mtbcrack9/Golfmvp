const DEFAULT={lat:49.0134,lon:12.1016};
// Rating-Daten: nur Werte mit externer Quellenangabe; OSM bleibt für Kartengeometrie.
const RATING_DB=[
 {match:['Golf- und Land-Club Regensburg','Golf- und Land-Club Regensburg e.V.'], tees:[{name:'Gelb Herren',gender:'Herren',par:72,cr:69.8,slope:133,length:5452},{name:'Rot Damen',gender:'Damen',par:72,cr:71.6,slope:128,length:4792}],source:'Leading Courses, abgerufen 2026-09-24'},
 {match:['Golfclub Regensburg-Sinzing','Gut Minoritenhof'], tees:[{name:'Gelb',gender:'Gemischt',par:58,cr:58.8,slope:94,length:2926}],source:'FlyAway Golf, abgerufen 2026-09-24'},
 {match:['Golf- und Landclub Schmidmühlen'], tees:[{name:'Herren',gender:'Herren',par:72,cr:70.7,slope:135,length:5757},{name:'Damen',gender:'Damen',par:72,cr:73.0,slope:127,length:5111}],source:'Golf in Bavaria, abgerufen 2026-09-24'}
];
function ratingFor(c){const n=(c?.name||'').toLowerCase();return RATING_DB.find(x=>x.match.some(m=>n.includes(m.toLowerCase())))||null}
function selectedTee(){return state.course?.rating?.tees?.find((_,i)=>i===Number(localStorage.getItem('golfmvp_tee')||0))||state.course?.rating?.tees?.[0]||null}
function setTee(i){localStorage.setItem('golfmvp_tee',String(i));state.course.rating=ratingFor(state.course);render();setTimeout(initMap,50)}
function courseHandicap(hi,tee){if(!tee||hi==='')return null;const n=Number(hi);if(!Number.isFinite(n))return null;return Math.round(n*(tee.slope/113)+(tee.cr-tee.par))}
function netScore(score,par,hcp,strokes){if(!score)return '—';return Number(score)-
function save(){localStorage.setItem('golfmvp_score',JSON.stringify(state.score));localStorage.setItem('golfmvp_rounds',JSON.stringify(state.rounds));localStorage.setItem('golfmvp_favorites',JSON.stringify(state.favorites))}
function go(t){state.tab=t;render()}
function render(){document.querySelectorAll('.tabbar button').forEach(b=>b.classList.toggle('active',b.dataset.tab===state.tab));const s=document.getElementById('screen');({home:home,round:round,score:score,stats:stats,courses:coursesView}[state.tab]||home)();}
function home(){document.getElementById('screen').innerHTML=`<div class="content"><section class="hero"><div class="eyebrow">ECHTE GOLFPLÄTZE · DEUTSCHLAND</div><h2>Bereit für die Runde?</h2><p>Suche Golfplätze, starte deine Runde und nutze GPS-Distanzen direkt auf dem iPhone.</p><button class="primary" onclick="findCourses()">⛳ Golfplatz auswählen</button></section><div class="section"><div class="sectiontitle"><h3>Deine Saison</h3><span class="muted">GolfMVP</span></div><div class="statsrow"><div class="stat"><b>${state.rounds.length}</b><small>Runden</small></div><div class="stat"><b>${avgScore()}</b><small>Ø Score</small></div><div class="stat"><b>${state.favorites.length}</b><small>Favoriten</small></div></div></div><div class="section"><div class="sectiontitle"><h3>Letzte Runde</h3></div>${state.rounds.length?`<div class="card row"><div><b>${esc(state.rounds.at(-1).course)}</b><div class="muted">${state.rounds.at(-1).date}</div></div><b>${state.rounds.at(-1).total}</b></div>`:`<div class="card muted">Noch keine gespeicherte Runde.</div>`}</div><div class="section card"><div class="sectiontitle"><h3>Dein Handicap</h3></div><div class="row"><label>Handicap Index</label><input class="handicapInput" type="number" step="0.1" value="${esc(localStorage.getItem('golfmvp_hi')||'') }" placeholder="z. B. 18,4" onchange="localStorage.setItem('golfmvp_hi',this.value)"></div><p class="muted">Damit berechnet GolfMVP das Course Handicap für den gewählten Abschlag.</p></div><div class="notice">Golfplatzdaten: OpenStreetMap. Je nach Platz können Loch-Geometrien, Par oder Pin-Daten unvollständig sein.</div></div>`}
function findCourses(){state.tab='courses';state.loading=true;render();locate().finally(()=>loadCourses())}
function locate(){return new Promise(resolve=>{if(!navigator.geolocation){resolve();return}navigator.geolocation.getCurrentPosition(p=>{state.gps=p.coords;resolve()},()=>resolve(),{enableHighAccuracy:true,timeout:8000,maximumAge:300000})})}
async function loadCourses(center=null){const p=center||state.gps||DEFAULT;const q=`[out:json][timeout:40];nwr[leisure=golf_course](around:60000,${p.lat??p.latitude},${p.lon??p.longitude});out center tags;`;try{const r=await fetch('https://overpass-api.de/api/interpreter?data='+encodeURIComponent(q));const j=await r.json();state.courses=(j.elements||[]).filter(x=>x.tags&&x.tags.name).map(x=>({id:x.id,type:x.type,name:x.tags.name,city:x.tags['addr:city']||x.tags['addr:town']||x.tags['addr:municipality']||'',lat:x.lat??x.center?.lat,lon:x.lon??x.center?.lon,holes:x.tags['golf:course']||x.tags['golf:holes']||'',website:x.tags.website||'',phone:x.tags.phone||''})).sort((a,b)=>distance(p,a)-distance(p,b));state.loading=false;render()}catch(e){state.loading=false;state.courses=[];render();toast('Golfplatzdaten konnten gerade nicht geladen werden.')}}
async function searchCourses(){const q=document.getElementById('courseSearch')?.value?.trim()||'';state.query=q;if(!q){loadCourses();return}state.loading=true;render();try{const r=await fetch('https://nominatim.openstreetmap.org/search?format=jsonv2&limit=1&countrycodes=de&q='+encodeURIComponent(q));const j=await r.json();if(!j.length){state.loading=false;render();toast('Ort oder Golfplatz nicht gefunden.');return}await loadCourses({lat:Number(j[0].lat),lon:Number(j[0].lon)})}catch(e){state.loading=false;render();toast('Suche nicht möglich.')}}
function isFav(c){return state.favorites.includes(String(c.id)+':'+c.type)}
function toggleFav(c){const k=String(c.id)+':'+c.type;state.favorites=isFav(c)?state.favorites.filter(x=>x!==k):[...state.favorites,k];save();render()}
function coursesView(){const p=state.gps||DEFAULT;const list=state.courses.filter(c=>!state.query||c.name.toLowerCase().includes(state.query.toLowerCase())||c.city.toLowerCase().includes(state.query.toLowerCase()));document.getElementById('screen').innerHTML=`<div class="content"><div class="sectiontitle"><div><div class="eyebrow">GOLFPLÄTZE</div><h2>Deutschland & Umgebung</h2></div><button class="smallbtn" onclick="loadCourses()">↻</button></div><div class="searchbox"><input id="courseSearch" value="${esc(state.query)}" placeholder="Ort oder Golfplatz suchen…" onkeydown="if(event.key==='Enter')searchCourses()"><button onclick="searchCourses()">Suchen</button></div>${state.loading?`<div class="card loading">Golfplätze werden gesucht …</div>`:list.length?list.slice(0,40).map(c=>`<div class="coursecard"><button class="coursemain" onclick="selectCourse('${c.id}','${c.type}')"><div><b>${esc(c.name)}</b><div class="muted">${esc(c.city||'Deutschland')} · ${Math.round(distance(p,c)/1000)} km · ${holesLabel(c.holes)}${ratingFor(c)?' · Slope '+ratingFor(c).tees[0].slope:''}</div></div><span>›</span></button><button class="fav" onclick='toggleFav(${JSON.stringify(c)})'>${isFav(c)?'★':'☆'}</button></div>`).join(''):`<div class="card muted">Keine Golfplätze gefunden. Suche z. B. nach „Regensburg“, „München“ oder einem Clubnamen.</div>`}<div class="notice">Quelle: OpenStreetMap. Golfplätze werden live über Overpass gesucht. Die Daten stehen unter der jeweiligen OSM-Lizenz.</div></div>`}
function holesLabel(v){const s=String(v||'');if(s.includes('18'))return '18 Loch';if(s.includes('9'))return '9 Loch';return 'Lochzahl n. a.'}
async function selectCourse(id,type){const c=state.courses.find(x=>String(x.id)===String(id)&&x.type===type);if(!c)return;state.course=c;state.course.rating=ratingFor(c);state.hole=1;state.loading=true;go('round');try{const q=`[out:json][timeout:45];(nwr[leisure=golf_course](around:3500,${c.lat},${c.lon});way[golf=hole](around:3500,${c.lat},${c.lon});node[golf=pin](around:3500,${c.lat},${c.lon});way[golf=green](around:3500,${c.lat},${c.lon});way[golf=fairway](around:3500,${c.lat},${c.lon});way[golf=bunker](around:3500,${c.lat},${c.lon});way[golf=water_hazard](around:3500,${c.lat},${c.lon}););out geom tags;`;const r=await fetch('https://overpass-api.de/api/interpreter?data='+encodeURIComponent(q));const j=await r.json();c.holeData=parseHoles(j.elements||[]);c.geometry=parseGeometry(j.elements||[],c);state.loading=false;render();setTimeout(initMap,50)}catch(e){c.holeData=[];c.geometry=[];state.loading=false;render();toast('Lochdaten nicht verfügbar – Platz bleibt auswählbar.')}}
function parseHoles(elements){const map={};for(const e of elements){const t=e.tags||{};if(t.golf==='hole'){const n=Number(t.ref);if(n>=1&&n<=18){const g=e.geometry||[];const end=g.at(-1);map[n]={...(map[n]||{}),par:Number(t.par)||null,handicap:Number(t.handicap)||null,dist:t['dist:yellow']||t['dist:white']||t.distance||null,lat:end?.lat??e.center?.lat,lon:end?.lon??e.center?.lon,tee:g[0]||null,path:g}}if(t.golf==='pin'){const n=Number(t.ref||t['hole:ref']);if(n>=1&&n<=18)map[n]={...(map[n]||{}),pin:{lat:e.lat,lon:e.lon}}}if(t.golf==='green'){const n=Number(t.ref||t['hole:ref']);if(n>=1&&n<=18){const g=e.geometry||[];if(g.length){const lat=g.reduce((a,x)=>a+x.lat,0)/g.length,lon=g.reduce((a,x)=>a+x.lon,0)/g.length;map[n]={...(map[n]||{}),green:{lat,lon}}}}}}return Object.keys(map).sort((a,b)=>a-b).map(n=>({n:Number(n),...map[n]}))}
function parseGeometry(elements,c){return elements.filter(e=>e.geometry?.length&&['fairway','bunker','water_hazard','green','hole'].includes(e.tags?.golf)).map(e=>({type:e.tags.golf,ref:e.tags.ref||'',geometry:e.geometry}))}
function initMap(){const el=document.getElementById('map');if(!el||!window.L||!state.course)return;state.map?.remove();state.map=L.map(el,{zoomControl:false}).setView([state.course.lat,state.course.lon],15);L.control.zoom({position:'bottomright'}).addTo(state.map);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'© OpenStreetMap contributors'}).addTo(state.map);const bounds=[];for(const g of state.course.geometry||[]){const pts=g.geometry.map(x=>[x.lat,x.lon]);pts.forEach(x=>bounds.push(x));const opts=g.type==='fairway'?{weight:4}:{weight:2};L.polyline(pts,opts).addTo(state.map)}for(const h of state.course.holeData||[]){const p=h.pin||h.green;if(p)L.marker([p.lat,p.lon]).addTo(state.map).bindTooltip('Loch '+h.n,{permanent:false})}if(state.gps){L.marker([state.gps.latitude,state.gps.longitude]).addTo(state.map).bindTooltip('Du',{permanent:true})}if(bounds.length)state.map.fitBounds(bounds,{padding:[20,20]})}
function currentHole(){return state.course?.holeData?.find(h=>h.n===state.hole)||null}
function round(){if(!state.course){document.getElementById('screen').innerHTML='<div class="content"><div class="card"><h3>Noch kein Golfplatz ausgewählt</h3><button class="primary" onclick="findCourses()">Golfplätze suchen</button></div></div>';return}const h=currentHole();const par=h?.par||4;const sc=state.score[state.hole]||'';const d=h?.pin||h?.green||{lat:h?.lat,lon:h?.lon};const rating=state.course.rating;const tee=selectedTee();const hi=localStorage.getItem('golfmvp_hi')||'';const ch=courseHandicap(hi,tee);const holeStrokes=ch?Math.floor(ch/18)+(h?.handicap&&((ch%18)>=h.handicap?1:0)):0;document.getElementById('screen').innerHTML=`<div class="content"><div class="roundtop"><div><div class="eyebrow">${esc(state.course.name).toUpperCase()}</div><div class="muted">${esc(state.course.city||'Deutschland')} · ${holesLabel(state.course.holes)}</div></div><span class="badge">${state.gps?'GPS AKTIV':'GPS bereit'}</span></div>${state.loading?'<div class="card loading">Loch-Daten werden geladen …</div>':''}${rating?`<div class="card ratingcard"><div class="sectiontitle"><h3>WHS Platzdaten</h3><span class="badge">${esc(tee?.name||'Abschlag')}</span></div><div class="teegrid">${rating.tees.map((t,i)=>`<button class="teebtn ${tee===t?'selected':''}" onclick="setTee(${i})"><b>${esc(t.name)}</b><small>CR ${t.cr} · Slope ${t.slope} · Par ${t.par}</small></button>`).join('')}</div>${hi?`<div class="row ratingrow"><span>Dein Course Handicap</span><b>${ch}</b></div>`:'<div class="muted">Handicap Index auf der Startseite eintragen.</div>}<div class="muted tiny">${esc(rating.source)}</div></div>`:''}<div class="holehead"><button class="navbtn" onclick="prevHole()">‹</button><div style="text-align:center"><div class="eyebrow">LOCH ${state.hole}</div><h2>Par ${par}</h2></div><button class="navbtn" onclick="nextHole()">›</button></div><div id="map" class="map"></div><div class="distances"><div class="distance"><small>PIN</small><b>${distanceLabel(h?.pin)}</b></div><div class="distance"><small>MITTE GRÜN</small><b>${distanceLabel(h?.green||d)}</b></div><div class="distance"><small>LOCHLÄNGE</small><b>${h?.dist?h.dist+' m':'—'}</b></div></div><div class="section card"><div class="row"><div><b>Score Loch ${state.hole}</b><div class="muted">Par ${par}${h?.handicap?' · HCP '+h.handicap:''}${ch?' · Vorgabe '+holeStrokes:''}</div></div><b>${sc||'—'}</b></div><div class="scoregrid" style="margin-top:12px">${[1,2,3,4,5,6,7,8,9].map(n=>`<button class="${sc===n?'selected':''}" onclick="setScore(${n})">${n}</button>`).join('')}</div><div style="margin-top:10px"><button class="primary" style="width:100%" onclick="locate().then(()=>{render();setTimeout(initMap,50)})">⌖ GPS aktualisieren</button></div></div>${state.hole===18?`<button class="primary" style="width:100%;margin-top:12px" onclick="finishRound()">Runde beenden & speichern</button>`:''}<div class="notice">Slope und Course Rating werden nicht aus OSM geschätzt. Wenn für einen Platz keine verifizierten Ratingdaten hinterlegt sind, zeigt GolfMVP diese Werte nicht an.</div></div>`} 
function distanceLabel(point){if(!point||!state.gps)return '—';return Math.round(haversine(state.gps.latitude,state.gps.longitude,point.lat,point.lon))+' m'}
function distance(a,b){return haversine(a.latitude??a.lat,a.longitude??a.lon,b.lat,b.lon)}
function haversine(a,b,c,d){const R=6371000,to=x=>x*Math.PI/180;const p1=to(a),p2=to(c),dp=to(c-a),dl=to(d-b);const x=Math.sin(dp/2)**2+Math.cos(p1)*Math.cos(p2)*Math.sin(dl/2)**2;return 2*R*Math.asin(Math.sqrt(x))}
function setScore(n){state.score[state.hole]=n;save();render();if(state.hole===18)toast('Score für Loch 18 gespeichert')}
function prevHole(){state.hole=Math.max(1,state.hole-1);render();setTimeout(initMap,50)}
function nextHole(){state.hole=Math.min(18,state.hole+1);render();setTimeout(initMap,50)}
function score(){const holes=Array.from({length:18},(_,i)=>i+1);const total=Object.values(state.score).reduce((a,b)=>a+Number(b),0);document.getElementById('screen').innerHTML=`<div class="content"><div class="sectiontitle"><h3>Scorecard</h3><span class="badge">${Object.keys(state.score).length}/18</span></div><div class="card"><table class="table"><thead><tr><th>Loch</th><th>Par</th><th>Score</th><th>+/-</th></tr></thead><tbody>${holes.map(n=>{const p=state.course?.holeData?.find(h=>h.n===n)?.par||4,s=state.score[n];return `<tr><td>${n}</td><td>${p}</td><td>${s||'—'}</td><td>${s?(s-p>0?'+':'')+(s-p):'—'}</td></tr>`}).join('')}</tbody></table><div class="bigscore"><div class="muted">Gesamt</div><b>${total||'—'}</b></div></div></div>`}
function finishRound(){const total=Object.values(state.score).reduce((a,b)=>a+Number(b),0);state.rounds.push({course:state.course?.name||'Golfplatz',total:total||0,date:new Date().toLocaleDateString('de-DE')});state.score={};save();toast('Runde gespeichert');go('home')}
function avgScore(){if(!state.rounds.length)return '—';return Math.round(state.rounds.reduce((a,r)=>a+r.total,0)/state.rounds.length)}
function stats(){const total=Object.values(state.score).reduce((a,b)=>a+Number(b),0);document.getElementById('screen').innerHTML=`<div class="content"><div class="sectiontitle"><h3>Statistiken</h3></div><div class="statsrow"><div class="stat"><b>${state.rounds.length}</b><small>Runden</small></div><div class="stat"><b>${avgScore()}</b><small>Ø Score</small></div><div class="stat"><b>${Object.keys(state.score).length}</b><small>Loch gespielt</small></div></div><div class="section card"><div class="row"><b>Aktuelle Runde</b><span class="badge">${total||'—'}</span></div><p class="muted">Die Score-Daten werden lokal auf deinem iPhone gespeichert.</p></div></div>`}
function showProfile(){toast('GolfMVP · OpenStreetMap · Deutschland')}
function esc(s){return String(s||'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function toast(t){const e=document.getElementById('toast');e.textContent=t;e.style.display='block';clearTimeout(window._toast);window._toast=setTimeout(()=>e.style.display='none',2500)}
if('serviceWorker' in navigator)navigator.serviceWorker.register('sw.js').catch(()=>{});render();
